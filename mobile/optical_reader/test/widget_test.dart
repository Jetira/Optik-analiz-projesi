import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without error', (WidgetTester tester) async {
    // Placeholder — ProviderScope + router gerektirdigi icin
    // entegrasyon testi Faz 6'da eklenecek.
    expect(1 + 1, equals(2));
  });
}
